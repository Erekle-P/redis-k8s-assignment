apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
    app.kubernetes.io/managed-by: Helm

spec:
  selector:
    app: redis

  ports:
    - name: tcp-redis
      port: 6379
      targetPort: 6379
